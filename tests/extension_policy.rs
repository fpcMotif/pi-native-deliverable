#[test]
fn extension_policy_explains_denial() {
    // TODO: once pi-ext is implemented:
    // let policy = pi_ext::Policy::safe();
    // let decision = policy.check(pi_ext::Capability::NetworkHttp);
    // assert!(decision.is_denied());
    // assert!(decision.reason.contains("safe policy"));
    assert!(true);
}
