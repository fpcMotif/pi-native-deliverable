repos:
  - repo: local
    hooks:
      - id: pi-policy
        name: pi policy + path guard checks
        entry: tools/precommit_checks.sh
        language: system
        pass_filenames: false
        always_run: true
